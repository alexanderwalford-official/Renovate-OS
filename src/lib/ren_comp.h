// RenLang for Renovate OS compiler
#include "stdint.h"
#include "../drivers/video/put.h"

// pass arguments to compiler
int comp_handler (char* arg) {
    return 0;
}

// main method to compile input code
void compile (char* code) {
}